<div class="all">
  <div class="header">
    <%= render "menu_bar"%>
  </div>
  
  <div class="main">
    <% if user_signed_in? %>
      <p id="notice"><%= notice %></p>



  <%= month_calendar events: @meetings do |date,meetings| %>
  <%= date%>

    <% meetings.each do |meeting| %>
      <div>
        <%=link_to meeting_path(meeting.id) do%>
        <%= meeting.name %>
        <% end %>
      </div>
    <% end %>
  <% end %>


  <br>
  <%= form_with model:@meeting, url:meetings_path(@meeting),local:true do |f| %>

    <div class="field">
      <%= f.label "予定" %>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label "開始時間" %>
      <%= f.datetime_select :start_time %>
    </div>

    <div class="actions">
      <%= f.submit"登録" %>
    </div>

  <% end %>
  <% end %>
</div>

  
