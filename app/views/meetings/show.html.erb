<p id="notice"><%= notice %></p>

<p>
  <strong>予定:</strong>
  <%= @meeting.name %>
</p>

<p>
  <strong>詳細:</strong>
  <%= @meeting.text%>
</p>

<%= link_to 'Edit', edit_meeting_path(@meeting.id) %> 
<%= link_to 'Destroy', meeting_path(@meeting), method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'Back', meetings_path %>
